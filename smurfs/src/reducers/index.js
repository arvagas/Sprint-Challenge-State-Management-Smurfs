const initialState = [
    {
        id: 0,
        name: 'Arvin',
        age: '210',
        height: '5.5cm'
    }
]

export const reducer = (state = initialState, action) => {
    switch (action.type) {
        default :
        return state
    }
}